// @ts-nocheck
// funciones para un futuro
/*
import { elements } from "./gameState.js";

export function setupButton(button, text, onClick, display = true) {
  button.innerText = text;
  button.onclick = onClick;
  button.style.display = display ? "inline-block" : "none";
}

export function setupButtons(buttons, texts, onClicks) {
  buttons.forEach((button, index) => {
    if (texts[index]) {
      setupButton(button, texts[index], onClicks[index]);
    } else {
      button.style.display = "none";
    }
  });
}

export function disableButtons() {
  Object.values(elements)
    .filter((el) => el.tagName === "BUTTON")
    .forEach((button) => (button.disabled = true));
}

export function enableButtons() {
  Object.values(elements)
    .filter((el) => el.tagName === "BUTTON")
    .forEach((button) => (button.disabled = false));
}
*/
